# ─── Supabase Database ────────────────────────────────────────────────
# Get these from: Supabase Dashboard → Settings → Database → Connection string
# Use the "Transaction" pooler (port 6543) for server actions
# Use the "Session" pooler (port 5432) for Prisma migrations/push
DATABASE_URL="postgresql://postgres.[PROJECT_REF]:[YOUR_PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[PROJECT_REF]:[YOUR_PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# ─── PostHog Analytics (Optional) ────────────────────────────────────────────
# Get these from: PostHog Dashboard → Project → Settings
# Leave empty to disable analytics
NEXT_PUBLIC_POSTHOG_KEY=""
NEXT_PUBLIC_POSTHOG_HOST="https://us.i.posthog.com"

# ─── Application Domain ───────────────────────────────────────────────────
# Your production domain
NEXT_PUBLIC_APP_URL="https://earlymark.ai"

# ─── Supabase Auth ────────────────────────────────────────────────────
# Get these from: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT_REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"
# Service role key for admin operations (keep secret!)
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# ─── AI Brain (Gemini) — Context-Heavy Tier ─────────────────────────
# Get this from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key"

# ─── DeepInfra (DeepSeek V3) — Logic-Heavy Tier ─────────────────────
# Get this from: https://deepinfra.com/dash/api_keys
DEEPINFRA_API_KEY="your-deepinfra-api-key"

# ─── Twilio (SMS / WhatsApp) ─────────────────────────────────────────
TWILIO_ACCOUNT_SID="your-twilio-account-sid"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_PHONE_NUMBER="+61400000000"
TWILIO_WHATSAPP_NUMBER="whatsapp:+14155238886"

# ─── Retell AI (Voice Calls — inbound + outbound) ──────────────────
# All voice calls (inbound & outbound) go through Retell AI.
# Get these from: https://www.retellai.com/dashboard
RETELL_API_KEY="your-retell-api-key"
RETELL_AGENT_ID="your-retell-agent-id"
RETELL_RESPONSE_ENGINE_ID="your-response-engine-id"
RETELL_PRIMARY_VOICE_ID="your-minimax-voice-id"
RETELL_FALLBACK_VOICE_ID="your-fallback-voice-id"

# ─── Stripe (Payments & Subscriptions) ────────────────────────────────
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_test_xxxxxxxxxxxx"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_xxxxxxxxxxxx"
# Webhook signing secret from: Stripe Dashboard → Webhooks → Signing Secret
STRIPE_WEBHOOK_SECRET="whsec_xxxxxxxxxxxx"
# Price IDs from: Stripe Dashboard → Products
# Intro: $60/month for first 3 months. Standard: $150/month after that.
STRIPE_PRO_INTRO_PRICE_ID="price_xxxxxxxxxxxx"
STRIPE_PRO_PRICE_ID="price_xxxxxxxxxxxx"

# ─── Resend (Email — Outbound + Inbound Webhooks) ──────────────────
# Get these from: https://resend.com/api-keys
RESEND_API_KEY="re_xxxxxxxxxxxx"
RESEND_FROM_DOMAIN="earlymark.ai"
# Signing secret from Resend Dashboard → Webhooks → Signing Secret
RESEND_WEBHOOK_SECRET="whsec_xxxxxxxxxxxx"
# Domain for lead-capture forwarding addresses: [alias]@INBOUND_LEAD_DOMAIN (default: inbound.earlymark.ai)
INBOUND_LEAD_DOMAIN="inbound.earlymark.ai"

# ─── Google OAuth (Sign-in + Gmail + Calendar) ─────────────────────────
# For "Sign in with Google", add these redirect URIs in Google Cloud Console:
#   https://earlymark.ai/api/auth/google-signin/callback
#   http://localhost:3000/api/auth/google-signin/callback
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ─── Google Maps (dashboard map view) ──────────────────────────────────
# Get an API key with Maps JavaScript API enabled: https://console.cloud.google.com/
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# ─── Microsoft Azure (Outlook + Calendar sync) ───────────────────────
AZURE_CLIENT_ID="your-azure-client-id"
AZURE_CLIENT_SECRET="your-azure-client-secret"

# ─── Xero Accounting ─────────────────────────────────────────────────
# Get these from: https://developer.xero.com/app/manage
XERO_CLIENT_ID="your-xero-client-id"
XERO_CLIENT_SECRET="your-xero-client-secret"
# Optional: dedicated key for encrypting OAuth tokens at rest (falls back to XERO_CLIENT_SECRET)
# TOKEN_ENCRYPTION_KEY="a-random-32-char-secret"

# ─── Cron Security ───────────────────────────────────────────────────────
# Secret for securing cron job endpoints
CRON_SECRET="your-cron-secret-key"

# ─── MYOB AccountRight ──────────────────────────────────────────────
# Get these from: https://developer.myob.com/
MYOB_CLIENT_ID="your-myob-client-id"
MYOB_CLIENT_SECRET="your-myob-client-secret"
MYOB_COMPANY_FILE_URI="https://ar1.api.myob.com/accountright/[COMPANY_FILE_ID]"
